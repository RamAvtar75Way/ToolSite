"use client";

import { MarkdownPreviewer } from "./MarkdownPreviewer";

// This is essentially the same as previewer but focused on source extraction
export function MarkdownToHtml() {
    return <MarkdownPreviewer />;
}
